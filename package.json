{
  "name": "ofoq-agent",
  "version": "6.0.0",
  "type": "module",
  "private": true,
  "description": "OFOQ General Agent — GitHub Actions + Firebase + Scheduling",
  "engines": { "node": ">=20" },
  "scripts": {
    "agent":     "node src/agent.js",
    "scheduler": "AGENT_MODE=scheduler node src/agent.js"
  },
  "dependencies": {
    "firebase-admin": "^12.3.0"
  }
}
